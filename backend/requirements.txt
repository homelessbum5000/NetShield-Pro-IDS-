fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0
httpx>=0.27,<1.0

# Optional real Kyber/ML-KEM key generation via liboqs.
# Requires liboqs to be built on the system (no prebuilt wheel on most platforms).
# Install separately; if absent, the backend falls back to a clearly-marked
# simulated keypair.
# python-oqs>=0.10,<1.0

# Optional: real quantum-circuit simulation on NVIDIA GPUs (set COMPUTE_PROVIDER=cudaq).
# Install separately per NVIDIA's CUDA-Q docs; not pinned here.
# cudaq

# Optional: real IBM Quantum jobs (set COMPUTE_PROVIDER=ibm).
# qiskit>=1.0
# qiskit-ibm-runtime>=0.20
